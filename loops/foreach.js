let names = ['Chris', 'Kevin', 'Naveed', 'Pete', 'Victor'];

names.forEach(function(name) {
  console.log(name);
});

// Another way to get the same result using an arrow function
names.forEach(name => console.log(name));